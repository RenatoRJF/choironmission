export interface LogoMobileProps {
  textColor: string;
  className: string;
  isLightTheme?: boolean;
  onToggleSwitch: () => void;
  switchBackgroundColor: string;
  circleBackgroundColor: string;
  colorTransitionClasses: string;
}